import { toSentence } from '../../../helpers/to-sentence';
import { module, test } from 'qunit';

module('Unit | Helper | to sentence');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = toSentence(42);
  assert.ok(result);
});
